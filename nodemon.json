{
  "$schema": "https://www.schemastore.org/nodemon.json",
  "exec": "pnpm wait-on:ng && pnpm build:electron && pnpm start:electron",
  "cwd": ".",
  "watch": ["projects/electron/*", "assets/*"],
  "ext": "ts,json,yml"
}
